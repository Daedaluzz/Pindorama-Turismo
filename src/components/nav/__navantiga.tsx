        {/* <li>
          <Link
            href="/destinations"
            className={`${styles.link} ${
              pathName === '/destinations'
                ? scrolling2
                  ? styles.scrolledActive
                  : styles.active
                : ''
            } ${scrolling2 ? styles.scrolledLink : ''}`}
          >
            {destinations}
          </Link>
        </li>
        <li>
          <Link
            href="/promotions"
            className={`${styles.link} ${
              pathName === '/promotions'
                ? scrolling2
                  ? styles.scrolledActive
                  : styles.active
                : ''
            } ${scrolling2 ? styles.scrolledLink : ''}`}
          >
            {promotions}
          </Link>
        </li>
        <li>
          <Link
            href="/packages"
            className={`${styles.link} ${
              pathName === '/packages'
                ? scrolling2
                  ? styles.scrolledActive
                  : styles.active
                : ''
            } ${scrolling2 ? styles.scrolledLink : ''}`}
          >
            {packages}
          </Link>
        </li>
        <li>
          <Link
            href="/contact"
            className={`${styles.link} ${
              pathName === '/contact'
                ? scrolling2
                  ? styles.scrolledActive
                  : styles.active
                : ''
            } ${scrolling2 ? styles.scrolledLink : ''}`}
          >
            {contact}
          </Link>
        </li>
        <li>
          <Link
            href="/about"
            className={`${styles.link} ${
              pathName === '/about'
                ? scrolling2
                  ? styles.scrolledActive
                  : styles.active
                : ''
            } ${scrolling2 ? styles.scrolledLink : ''}`}
          >
            {about}
          </Link>
        </li>
        <li>
          <Link
            href="/management"
            className={`${styles.link} ${
              pathName === '/management'
                ? scrolling2
                  ? styles.scrolledActive
                  : styles.active
                : ''
            } ${scrolling2 ? styles.scrolledLink : ''}`}
          >
            {management}
          </Link>
        </li> */}